import React from "react";
import LatestProject from "./LatestProject";

const LogisticsProject = () => {
  const project = { title: "Logistics" };
  return (
    <div>
      <LatestProject project={project} />
    </div>
  );
};

export default LogisticsProject;
