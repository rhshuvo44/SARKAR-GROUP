import LatestProject from './LatestProject';

const NavigationProjects = () => {
    const project = { title: "Navigation" };
    return (
        <div>
          <LatestProject project={project} />  
        </div>
    );
};

export default NavigationProjects;